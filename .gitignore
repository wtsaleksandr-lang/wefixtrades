node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.keyword-cache.json
tests/e2e/admin-crm/.auth/
.claude/worktrees/
playwright-report/
test-results/
audit-report/
audit-evidence/

# Runtime-generated email chart images
data/email-charts/*
!data/email-charts/.gitkeep

# preview-transactional-emails.ts output (regenerable via npx tsx)
data/email-previews/

# User-uploaded deliverable files
data/uploads/

# Local scratchpad + dev artifacts (not for the repo)
.scratch/
.tmp/
dev-server.log

# Claude Code runtime state
.claude/scheduled_tasks.lock

# Overnight audit / preview working artifacts (regenerable; not source).
# Root-level screenshots + audit shot dirs + audit reports/scripts produced by
# the idle-time page-audit loop — keep them out of commits so they never get
# swept in by accident.
/*.png
/audit-shots/
/mkt-shots/
/audit-report.jsonl
/mkt-report.jsonl
/run-audit.mjs
/run-mkt-audit.mjs
/nocache-server.mjs
/OVERNIGHT-FINDINGS.md

# orchestrator scratch (screenshots, audit scripts, preview server) — never commit
_audit/
_diag/
_dpolish/
_hunt/
_live/
_screenshots/
_static.mjs
_m/
_mf/
_tf/
_theme/
_tv/
_v/
_p0/
_p0b/
_p1/
_p2/
_vr*/
_vr6_probe.html
_vr6_smoke.mjs

# local dev junk (serve/build/tunnel logs + roof-quote spike scratch)
_*.log
spikes/roof-quote/serve.*.log
spikes/roof-quote/verify-*.cjs
spikes/roof-quote/.launch.sh
spikes/roof-quote/feat.json
shadowshot-*.png
heatmap-*.png

# Roof-Quote runtime disk cache (Solar buildingInsights/dataLayers, captures, AI renders) — never commit
.cache/roofquote/
spikes/roof-quote/proof-*.mjs
spikes/roof-quote/proof-*.mts
spikes/roof-quote/proof-*.cjs
_rec/

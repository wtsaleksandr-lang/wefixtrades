# ship

Analyze the entire repository first.

Then plan the safest implementation.

Then implement the changes with minimal edits.

Run relevant build, lint, or test commands.

If errors appear, debug and fix them before stopping.

Stop only when the task is completed or truly blocked.